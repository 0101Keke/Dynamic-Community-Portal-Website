<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Events</title>
</head>


<!--<script src="/models/Event.js"></script>-->
<div class="container">
  <h1>Upcoming Gaming Events in South Africa</h1>
  <p>Check out the exciting gaming events happening soon!</p>

  <% if (events && events.length > 0) { %>
    <% events.forEach(event => { %>
      <div class="card">
        <h3><%= event.name %></h3>
        <p><%= event.dates %> | <%= event.location %></p>
      </div>
    <% }) %>
  <% } else { %>
    <p>No upcoming gaming events currently listed.</p>
  <% } %>
</div>

<%- include('partials/footer') %>
</html>